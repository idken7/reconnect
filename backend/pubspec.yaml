name: reconnect_backend
description: Local HTTP backend for the Reconnect app MVP.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  crypto: ^3.0.3

dev_dependencies:
  test: ^1.26.0
